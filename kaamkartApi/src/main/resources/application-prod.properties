# Production Profile Configuration
# Use this profile for production deployment
# IMPORTANT: Override sensitive values using environment variables or external configuration

# Server Configuration
# Render uses PORT environment variable, Spring Boot uses server.port
server.port=${PORT:${SERVER_PORT:8585}}

# Database Configuration (Use environment variables in production)
# Production database connection (can be overridden by environment variables)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d4ttu5pr0fns739f4s40-a:5432/kaamkart}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:kaamkart_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:u7B1yZpeSNCFzrsxv2ty8FYmMf4AjF7F}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connection Pool Configuration (HikariCP)
# Optimized for millions of users - adjust based on server capacity
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.minimum-idle=20
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.leak-detection-threshold=60000
# For high load: increase maximum-pool-size to 100-200

# Query Cache Configuration (Disabled - requires additional dependencies)
# spring.jpa.properties.hibernate.cache.use_second_level_cache=true
# spring.jpa.properties.hibernate.cache.use_query_cache=true
# spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.jcache.JCacheRegionFactory
# spring.jpa.properties.hibernate.javax.cache.provider=org.ehcache.jsr107.EhcacheCachingProvider
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Jackson Configuration
spring.jackson.deserialization.fail-on-unknown-properties=false

# JWT Configuration (Use environment variable in production)
jwt.secret=${JWT_SECRET:H7x83kpU4jwgG9PBPTRI4eWbaA7CqXrPq0CjN2yMbzU=}
jwt.expiration=${JWT_EXPIRATION:604800000}

# CORS Configuration (Production - restrict to your frontend domain)
# Set CORS_ALLOWED_ORIGINS environment variable in Render dashboard
# Default includes production frontend URL
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://nokariya.onrender.com,https://kaamkart.in,https://www.kaamkart.in}

# WebSocket Configuration
# Set WEBSOCKET_ALLOWED_ORIGINS environment variable in Render dashboard
# Default includes production frontend URL
websocket.allowed-origins=${WEBSOCKET_ALLOWED_ORIGINS:https://nokariya.onrender.com,https://kaamkart.in,https://www.kaamkart.in}

# Logging Configuration
logging.level.com.kaamkart=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.file.name=${LOG_FILE_PATH:/app/logs/kaamkart-api.log}
logging.file.max-size=10MB
logging.file.max-history=30
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Error Handling
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-binding-errors=never

