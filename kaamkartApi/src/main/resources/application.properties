# Default Profile (Development)
# To use production profile: java -jar app.jar --spring.profiles.active=prod
# Or set environment variable: SPRING_PROFILES_ACTIVE=prod

spring.profiles.active=dev

# Default values (can be overridden by profile-specific files)
server.port=8585
